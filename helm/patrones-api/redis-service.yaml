apiVersion: v2
kind: Service
metadata:
  name: "{{ .Chart.Name }}-redis"
  labels:
    app: redis-patrones
spec:
  type: ClusterIP
  selector:
    app: "{{ .Chart.Name }}-redis"
  ports:
    - protocol: TCP
      port: {{ .Values.redis.port }}
      targetPort: {{ .Values.redis.port }}